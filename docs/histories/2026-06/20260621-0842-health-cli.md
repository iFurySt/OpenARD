## [2026-06-21 08:42] | Task: Health CLI

### Execution Context

- Agent ID: `codex`
- Base Model: `GPT-5`
- Runtime: `Codex CLI`

### User Query

> Continue building the Go/Cobra/GORM/Gin/Postgres ARD registry and management toolkit
> with real verification, milestone commits, and strict alignment with the ARD spec.

### Changes Overview

- Area: CLI and public registry operations
- Key actions:
  - Added `ard health` and `ardctl health` for the public `/health` endpoint.
  - Supported text output for humans and `--json` output for automation.
  - Added unit coverage for text output, JSON output, HTTP errors, and command routing.
  - Updated the real E2E artifact workflow to check a live registry through
    `ardctl health --json`.
  - Updated README, architecture, reliability, quality, and release documentation.

### Design Intent

The Go SDK already exposed the public health endpoint, while CLI users still had to use
`curl`. This change aligns CLI and SDK capabilities without making health an admin-only
operation. The command remains unauthenticated and uses the existing public `/health`
contract.

### Files Modified

- `internal/cli/health.go`
- `internal/cli/health_test.go`
- `internal/cli/root.go`
- `internal/cli/root_test.go`
- `scripts/test-e2e-artifacts.sh`
- `README.md`
- `docs/ARCHITECTURE.md`
- `docs/RELIABILITY.md`
- `docs/QUALITY_SCORE.md`
- `docs/releases/feature-release-notes.md`
